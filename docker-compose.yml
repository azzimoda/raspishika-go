services:
  raspishika:
    image: raspishika
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./.env:/app/.env
      - ./database:/app/database
      - ./configs:/app/configs
      - ./storage/logs:/app/storage/logs
